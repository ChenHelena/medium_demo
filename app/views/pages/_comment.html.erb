<article class="comment">
  <span class="profile">
    <%= avatar(current_user,size: { x: 50, y: 50 }) %>
    <span class="username"><%= @story.user.username %></span>
  </span>
  <%= simple_format(comment.content) %>
</article>